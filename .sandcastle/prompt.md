Follow the workflow prescribed by `$ask-matt`.

Communicate with the user in Italian.

From `{{ISSUES_PATH}}`, select the first implementable ticket.

Explicitly invoke the `$implement` skill for the selected ticket and carry it
through the prescribed workflow.

Work on exactly one ticket during this iteration.

If no ticket is implementable, output:

<promise>COMPLETE</promise>
