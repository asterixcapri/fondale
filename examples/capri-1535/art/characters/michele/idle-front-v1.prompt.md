# Michele idle front v1

Use case: stylized-concept

Asset type: four-frame front-facing idle-loop Art Master

Input images: `michele-turnaround-v4.png` is the exact Character identity,
costume, scale, asymmetry, palette, light, and stable-pose reference. No earlier
sprite is a reference.

Primary request: exactly four front-facing frames in a strict 2×2 grid, ordered
left-to-right top row then left-to-right bottom row: stable neutral pose, tiny
inhale with restrained shoulder/chest rise, blink with tiny weight relaxation,
neutral exhale/settle returning into frame 1.

Keep both feet fully planted and the Ground Point identical. At 100 px the body
displacement must remain sub-pixel; the idle should never resemble swaying,
bobbing, stepping, talking, or a dramatic pose. Preserve the approved 1530
costume and physical-side accessories exactly.

Exactly one complete figure per cell with wide flat-green gutters; uniform
`#00ff00`; fixed upper-left light; no labels, borders, grid lines, shadows,
scenery, props, blur, text, watermark, or identity drift.

