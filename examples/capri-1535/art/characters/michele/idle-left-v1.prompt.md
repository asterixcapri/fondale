# Michele idle left v1

Use case: stylized-concept

Asset type: four-frame left-facing idle-loop Art Master

Input images: `michele-turnaround-v4.png` is the exact Character identity,
costume, scale, asymmetry, palette, light, and stable-pose reference. No earlier
sprite is a reference.

Primary request: exactly four left-facing profile frames in a strict 2×2 grid,
ordered left-to-right top row then left-to-right bottom row: stable neutral
pose, tiny inhale, blink with minute shoulder relaxation, neutral exhale/settle
returning into frame 1. Every figure faces screen left; never mirror right.

Keep both feet fully planted and Ground Point identical. At 100 px displacement
is sub-pixel; no swaying, bobbing, stepping, talking, or turning. Preserve the
approved 1530 costume, true accessory sides, and fixed upper-left light.

Exactly one complete figure per cell with wide flat-green gutters; uniform
`#00ff00`; no labels, borders, grid lines, shadows, scenery, props, blur, text,
watermark, or identity drift.

