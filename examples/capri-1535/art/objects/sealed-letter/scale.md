# Sealed letter scale

The sealed letter uses `master.png` as its lossless Art Master. Its fitted
Runtime presentations are a `42×25` Scene asset with a bottom-centre Visual
Anchor and a separately composed `32×32` Inventory Appearance. Raffaele's
explicit handover moves that same Object into Inventory exactly once.

`engine-scale-check.png` is the `1280×720` actual-play-size Engine diagnostic.
It shows the letter and oil flask together at their native Inventory sizes over
the restored harbour Scene after Continue.
